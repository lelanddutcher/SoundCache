FROM python:3.13-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src

RUN pip install --no-cache-dir fastapi uvicorn pydantic

ENV PYTHONPATH=/app/src
EXPOSE 43117
CMD ["python", "-m", "sound_vault.relay.server"]
